# GFIN Migrations Dockerfile
# One-shot container that runs database migrations

FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY packages/ ./packages/

CMD ["python", "-m", "packages.common.run_migrations"]
