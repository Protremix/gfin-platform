# GFIN Python Dependencies
# Layer A (current): pytest, pydantic, structlog, ruff, mypy
# Layer B (pilot): fastapi, uvicorn, sqlalchemy, asyncpg, httpx

# Core
pydantic>=2.0
structlog>=24.0

# API Layer (Layer B - pilot)
fastapi>=0.100
uvicorn[standard]>=0.24
httpx>=0.25

# Database Layer (Layer B - pilot)
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13

# AI Gateway
openai>=1.0

# Testing
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=4.1

# Code Quality
ruff>=0.1
mypy>=1.7
