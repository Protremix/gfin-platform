# GFIN Knowledge Assessment & Examination Suite

**Document Version:** 1.0  
**Passing Threshold:** 80% per role track (8/10 correct minimum per role)  
**Total Questions:** 40 Questions (10 Operator, 10 Investigator, 10 Administrator, 10 Developer)  

---

## Part 1: Operator Assessment Questions (Questions 1–10)

#### Q1 (Scenario): An operator checks the system health endpoint and receives a status of `DEGRADED` with message `event_bus: DLQ message count exceeds threshold (142 messages)`. What is the immediate operational procedure?
- A) Restart the entire Kubernetes cluster immediately.
- B) Inspect DLQ messages using `get_dlq_messages()`, identify the failing subscriber topic, fix underlying downstream blockage, and execute `replay_dlq()`.
- C) Delete all messages in the Dead Letter Queue to clear the alert.
- D) Re-route event bus traffic to external internet APIs.

#### Q2: What are the four valid states of the Campaign Engine lifecycle?
- A) INIT, RUNNING, PAUSED, STOPPED
- B) DRAFT, ACTIVE, DORMANT, DISMANTLED
- C) NEW, OPEN, CLOSED, ARCHIVED
- D) SUBMITTED, REVIEWED, APPROVED, DENIED

#### Q3: Which hash algorithm is used by the Evidence Vault to ensure tamper-evident integrity?
- A) MD5
- B) SHA-1
- C) SHA-256
- D) CRC32

#### Q4 (Scenario): After a new deployment, P95 API latency rises to 4,500ms (SLO < 200ms) and error rate reaches 8%. According to Runbook 2, what action should be taken?
- A) Wait 24 hours for memory cache to warm up.
- B) Immediately trigger Rollback Runbook (`helm rollback gfin`) and verify health.
- C) Scale up CPU resources without rolling back code.
- D) Disable authentication rate limiting.

#### Q5: In GFIN Layer A in-memory execution, where are logs and state flushed during graceful shutdown?
- A) State is discarded with zero cleanup.
- B) Pending event queues are flushed and evidence vault state is checked for cryptographic integrity before process termination.
- C) Flushed directly to public AWS S3 buckets over unencrypted HTTP.
- D) Written to unencrypted text files in `/tmp`.

#### Q6: How many default Kafka topics are configured on the GFIN Event Bus?
- A) 5
- B) 14
- C) 28
- D) 50

#### Q7: What is the retention period for evidence classified as `LAW_ENFORCEMENT` under GFIN compliance rules?
- A) 1 Year
- B) 5 Years
- C) 10 Years
- D) 20 Years

#### Q8: What priority level is assigned to an alert reporting a "Critical Transnational Zero-Day Phishing Infrastructure Cluster"?
- A) P3_LOW
- B) P2_MEDIUM
- C) P1_HIGH
- D) P0_CRITICAL

#### Q9 (Scenario): An operator observes duplicate domain entities in the system (`fraud-bank.com` and `FRAUD-BANK.COM`). Which component resolves these into a single primary entity?
- A) Cryptographic Evidence Vault
- B) Entity Resolution Engine (Module 04) using normalizers
- C) Model Gateway
- D) Disaster Recovery Engine

#### Q10: What command checks the health of all GFIN components in Python?
- A) `pytest tests/health`
- B) `HealthCheckRegistry().check_all()`
- C) `gfin --status`
- D) `systemctl status gfin`

---

## Part 2: Investigator Assessment Questions (Questions 11–20)

#### Q11: What composite score threshold marks a fraud report as HIGH priority during automated triage in Module 14?
- A) Score >= 20.0
- B) Score >= 50.0
- C) Score >= 75.0
- D) Score >= 99.0

#### Q12: Name the 7 sequential stages of the GFIN Cross-Border Request Workflow (Module 26).
- A) START, CHECK, APPROVE, TRANSFER, RECEIVE, LOG, END
- B) SUBMIT, VALIDATE, AUTHORIZE, ROUTE, REVIEW, DECIDE, CLOSE
- C) DRAFT, PENDING, ACTIVE, DORMANT, EXPIRED, DELETED, ARCHIVED
- D) INGEST, PARSE, SCORE, LINK, ALERT, NOTIFY, DISMISS

#### Q13 (Scenario): An investigator needs to trace $500,000 in stolen cryptocurrency moving across multiple mixer wallets on Ethereum. Which algorithm and service should be used?
- A) Depth-First Search via Web Crawler
- B) Breadth-First Search (BFS) fund tracing via `CryptoIntelligenceService`
- C) Levenshtein fuzzy search via Search Platform
- D) Random walk simulation via Model Gateway

#### Q14: How many authorized investigation tools are registered in the AI Investigation Orchestrator?
- A) 5
- B) 10
- C) 15
- D) 25

#### Q15: Which role level is required to execute the `create_alert` and `request_information` tools in the AI Orchestrator?
- A) CITIZEN
- B) ANALYST
- C) INVESTIGATOR
- D) SUPERVISOR

#### Q16: How does the AI Investigation Orchestrator handle claims generated by AI tools?
- A) Accepts all AI claims as ground truth automatically.
- B) Maps claims to backing evidence and flags unverified claims for human investigator review (`requires_human_review=True`).
- C) Deletes claims that do not match internet search results.
- D) Sends claims directly to public social media.

#### Q17: What export format is standard in GFIN for sharing threat intelligence bundles with external law enforcement partners?
- A) CSV Spreadsheet
- B) STIX 2.1 JSON
- C) Plain Text PDF
- D) XML Document

#### Q18: Which relationship type connects a domain entity to its hosting IP address in the GFIN property graph?
- A) `REGISTERED_BY`
- B) `RESOLVES_TO`
- C) `TRANSITIONED_TO`
- D) `OWNED_BY`

#### Q19 (Scenario): An investigator submits a Cross-Border Request to Jurisdiction B. Jurisdiction B decides to share entity indicators but redact confidential informant identities. What decision type is recorded?
- A) APPROVE
- B) PARTIAL
- C) DENY
- D) REJECT

#### Q20: What is the deduplication similarity threshold above which incoming fraud reports are automatically linked to a primary report?
- A) 0.50
- B) 0.65
- C) 0.80
- D) 0.99

---

## Part 3: Administrator Assessment Questions (Questions 21–30)

#### Q21: How many explicit resources and actions are defined in the GFIN Access Control Matrix?
- A) 5 Resources x 5 Actions
- B) 9 Resources x 8 Actions
- C) 12 Resources x 10 Actions
- D) 20 Resources x 15 Actions

#### Q22: Can a user with the `analyst` role execute the `delete` action on `evidence` resources under the Access Control Matrix?
- A) Yes, always.
- B) No, `analyst` is restricted to `read` and `create` on evidence.
- C) Only during night shifts.
- D) Yes, if approved by a citizen.

#### Q23: Which role in GFIN has explicit permission to manage users (`user_management` resource)?
- A) `citizen`
- B) `analyst`
- C) `investigator`
- D) `administrator`

#### Q24: What are the 5 Data Classification levels in GFIN, ordered from lowest to highest security?
- A) LOW, MEDIUM, HIGH, CRITICAL, TOP_SECRET
- B) PUBLIC, COMMUNITY, RESTRICTED, LAW_ENFORCEMENT, HIGHLY_RESTRICTED
- C) UNCLASSIFIED, SECRET, TOP_SECRET, SCI, COSMIC
- D) LEVEL_1, LEVEL_2, LEVEL_3, LEVEL_4, LEVEL_5

#### Q25 (Scenario): Under ABAC policy, a Law Enforcement Officer from US jurisdiction attempts to access `LAW_ENFORCEMENT` classified evidence owned by German jurisdiction without a cross-border request authorization. What is the authorization decision?
- A) ALLOW (All law enforcement can see all global data)
- B) DENY (Jurisdiction mismatch blocks access for LE data)
- C) ALLOW with warning
- D) PENDING citizen review

#### Q26: What is the scheduled rotation frequency for JWT signing keys according to Runbook 3?
- A) Every 24 hours
- B) Every 30 days
- C) Every 90 days
- D) Every 365 days

#### Q27: What are the Recovery Time Objective (RTO) and Recovery Point Objective (RPO) targets defined for GFIN Disaster Recovery?
- A) RTO < 24 hours, RPO < 1 hour
- B) RTO < 1 hour, RPO < 5 minutes
- C) RTO < 10 minutes, RPO = 0 seconds
- D) RTO < 7 days, RPO < 24 hours

#### Q28: During a GDPR erasure procedure, what happens to historical audit logs referencing the erased subject?
- A) Audit logs are deleted completely.
- B) Audit logs are preserved but anonymized to prevent cryptographically breaking the audit chain.
- C) Audit logs are published to a public website.
- D) Audit logs are encrypted and emailed to the user.

#### Q29: What tool is used to manage dynamic database secrets and PKI certificates in production?
- A) Redis
- B) HashiCorp Vault
- C) Apache Kafka
- D) OpenSearch

#### Q30: What command checks the cryptographic chain integrity of the security audit log?
- A) `AuditLogger().verify_audit_chain()`
- B) `git log --verify`
- C) `sha256sum /var/log/audit.log`
- D) `kubectl logs gfin-audit`

---

## Part 4: Developer Assessment Questions (Questions 31–40)

#### Q31: What is the primary architectural difference between Layer A and Layer B implementations in GFIN?
- A) Layer A is written in C++, Layer B is written in Python.
- B) Layer A uses zero-external-dependency in-memory adapters for fast execution/testing, while Layer B connects to production infrastructure (PostgreSQL, Kafka, Neo4j, Vault).
- C) Layer A runs on mobile phones, Layer B runs on mainframe computers.
- D) Layer A is deprecated and no longer used.

#### Q32: All canonical domain entity schemas in GFIN inherit from which base class?
- A) `dict`
- B) `BaseEntity` (inheriting from Pydantic `BaseModel`)
- C) `DjangoModel`
- D) `SQLAlchemyBase`

#### Q33: In the GFIN package hierarchy, which statement regarding package dependencies is correct?
- A) `schemas` depends on `services` and `observability`.
- B) `services` depends on `schemas`, `common`, `auth`, and `events`; `schemas` has zero internal dependencies.
- C) All packages import each other circularly.
- D) `observability` is imported by `schemas`.

#### Q34: How are stable entity primary keys formatted in GFIN?
- A) Sequential integers (1, 2, 3...)
- B) User email addresses
- C) Type-prefixed UUIDs (e.g. `ENT-DOMAIN-a1b2c3d4...`)
- D) Random 4-digit numbers

#### Q35 (Scenario): A developer needs to create a new fraud detection rule. Which class should the rule inherit from, and where should it be registered?
- A) Inherit from `BaseEntity` and register in `EvidenceVault`.
- B) Inherit from `FraudRule` and register in `FraudDetectionEngine`.
- C) Inherit from `HealthCheck` and register in `MetricsCollector`.
- D) Inherit from `Event` and register in `EventBus`.

#### Q36: What static linter and formatting tool is mandated for all Python code in GFIN?
- A) Flake8
- B) Black
- C) Ruff
- D) Pylint

#### Q37: What is the maximum allowed line length enforced by Ruff in GFIN?
- A) 80 characters
- B) 100 characters
- C) 120 characters
- D) Unlimited

#### Q38: What command runs the full automated test suite with coverage report?
- A) `python run_tests.py`
- B) `pytest -v --cov=packages --cov=services`
- C) `npm test`
- D) `cargo test`

#### Q39: In GFIN, why are mutable fields like email addresses, phone numbers, or IP addresses never used as primary keys?
- A) Python does not support string keys.
- B) Real-world identifiers change, merge, or re-assign over time; stable UUID-based keys preserve referential integrity and historical provenance.
- C) It is forbidden by internet standards.
- D) String keys consume too much memory.

#### Q40: What module in GFIN evaluates the 12 production go/no-go quality and readiness criteria?
- A) Module 01 (Dev Environment)
- B) Module 05 (Event Bus)
- C) Module 22 (AI Orchestrator)
- D) Module 40 (`packages.production.go_no_go_gates`)

---

## Answer Key & Rationales

### Operator Answers
1. **B** — DLQ messages must be inspected, underlying downstream bugs resolved, and replayed safely using `replay_dlq()`.
2. **B** — The four lifecycle states are `DRAFT`, `ACTIVE`, `DORMANT`, and `DISMANTLED`.
3. **C** — SHA-256 is the standard cryptographic hash algorithm used across the Evidence Vault.
4. **B** — Latency > 2x SLO and error rate > 5% are mandatory rollback triggers under Runbook 2.
5. **B** — Graceful shutdown flushes event queues and verifies vault hash chain integrity.
6. **B** — The event bus defines 14 standard Kafka topics.
7. **C** — `LAW_ENFORCEMENT` classified evidence has a mandatory 10-year retention period.
8. **D** — Critical zero-day threat clusters are categorized as `P0_CRITICAL`.
9. **B** — The Entity Resolution Engine normalizes and deduplicates candidate raw inputs.
10. **B** — `HealthCheckRegistry().check_all()` executes all component health checks.

### Investigator Answers
11. **C** — Composite score >= 75.0 triggers HIGH priority triage classification.
12. **B** — The 7 stages are SUBMIT, VALIDATE, AUTHORIZE, ROUTE, REVIEW, DECIDE, CLOSE.
13. **B** — Multi-hop fund flow analysis uses Breadth-First Search (BFS) in `CryptoIntelligenceService`.
14. **C** — Module 22 registers 15 controlled investigation tools.
15. **D** — `create_alert` and `request_information` require `SUPERVISOR` role authorization.
16. **B** — Claims are linked to evidence and unverified claims are flagged for human review (`requires_human_review=True`).
17. **B** — STIX 2.1 JSON is the standard intelligence bundle export format.
18. **B** — `RESOLVES_TO` links domain entities to IP address entities.
19. **B** — Sharing subset data with redactions is classified as a `PARTIAL` decision.
20. **C** — A similarity score > 0.80 triggers automatic duplicate report linking.

### Administrator Answers
21. **B** — The matrix defines 9 Resources x 8 Actions.
22. **B** — `analyst` is restricted to `read` and `create` on evidence; `delete` is denied.
23. **D** — Only `administrator` has permissions on the `user_management` resource.
24. **B** — The 5 levels are PUBLIC, COMMUNITY, RESTRICTED, LAW_ENFORCEMENT, HIGHLY_RESTRICTED.
25. **B** — Jurisdiction mismatch blocks access to `LAW_ENFORCEMENT` classified data without explicit cross-border request approval.
26. **B** — JWT signing keys are rotated every 30 days per Runbook 3.
27. **B** — GFIN DR targets are RTO < 1 hour and RPO < 5 minutes.
28. **B** — Audit logs are anonymized to maintain hash chain continuity without retaining PII.
29. **B** — HashiCorp Vault manages dynamic credentials and PKI certs.
30. **A** — `AuditLogger().verify_audit_chain()` verifies cryptographic hash linkages in audit logs.

### Developer Answers
31. **B** — Layer A provides in-memory mock adapters; Layer B connects to production infrastructure.
32. **B** — `BaseEntity` is the canonical base model inheriting from Pydantic `BaseModel`.
33. **B** — `schemas` has zero internal dependencies; `services` depends on lower layers cleanly.
34. **C** — Primary keys are stable, type-prefixed UUIDs (e.g., `ENT-DOMAIN-...`).
35. **B** — Fraud rules inherit from `FraudRule` and register in `FraudDetectionEngine`.
36. **C** — Ruff is the mandated static linter and code formatter.
37. **B** — Line length is capped at 100 characters.
38. **B** — `pytest -v --cov=packages --cov=services` executes the test suite with coverage.
39. **B** — Real-world identifiers mutate; stable UUID keys maintain integrity over time.
40. **D** — `packages.production.go_no_go_gates` evaluates all 12 production readiness gates.

---

## Knowledge Assessment Sign-Off Template

```
================================================================────────────────
                  GFIN KNOWLEDGE ASSESSMENT SIGN-OFF RECORD
================================================================────────────────

Candidate Name:     ____________________________________________________
Candidate Role:     [ ] Operator  [ ] Investigator  [ ] Administrator  [ ] Developer
Agency / Org:       ____________________________________________________

EXAMINATION RESULTS SUMMARY:
  Part 1 Score (Operator):        ______ / 10
  Part 2 Score (Investigator):    ______ / 10
  Part 3 Score (Administrator):   ______ / 10
  Part 4 Score (Developer):       ______ / 10

  TOTAL SCORE ACHIEVED:          ______ / 40  (______ %)
  PASS THRESHOLD:                32 / 40 (80%) Minimum

  VERDICT:                        [ ] PASSED - CERTIFIED  [ ] FAILED - RETAKE REQUIRED

EXAMINER SIGNATURE:

Examiner Name:      ____________________________________________________
Examiner Title:     ____________________________________________________
Signature:          ____________________________________________________
Date:               ____________________
================================================================────────────────
```
