"""GFIN Packages — Core Libraries.

This package contains the shared interfaces and schemas used across all GFIN services.

Structure:
    - schemas/ — Core domain types, enums, base models
    - common/ — Infrastructure abstraction interfaces (database, event bus, search, storage, graph, cache, model gateway, identity)
    - events/ — Event schemas and topic definitions
    - observability/ — Logging, metrics, tracing interfaces
    - auth/ — Authentication and authorization interfaces
"""
